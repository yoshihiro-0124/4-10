<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand" href="/"><span>Bookers</span></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
           <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
           <ul class="navbar-nav ml-auto">
             <% if user_signed_in? %>
              <li><%= link_to user_path(current_user),class:"fas fa-home nav-item nav-link text-light" do %> Home<% end %></li>
              <li><%= link_to users_path,class:"fas fa-users nav-item  nav-link text-light" do %> Users<% end %></li>
              <li><%= link_to books_path,class:"fas fa-book-open nav-item nav-link text-light" do %> Books<% end %></li>
              <li><%= link_to destroy_user_session_path,class:"fas fa-sign-out-alt nav-item nav-link text-light",method: :delete do %> logout<% end %></li>
             <% else %>
              <li><%= link_to :root,class:"fas fa-home nav-item nav-link text-light" do %> Home<% end %></li>
              <li><%= link_to :about,class:"fas fa-link nav-item nav-link text-light" do %> About<% end %></li>
              <li><%= link_to new_user_registration_path,class:"fas fa-user-plus nav-item nav-link text-light" do %> sign up<% end %></li>
              <li><%= link_to new_user_session_path,class:"fas fa-sign-in-alt nav-item nav-link text-light" do %> login<% end %></li>
             <% end %>
           </ul>
          </div>
        </div>
      </nav>
   </header>

   <main>
       <% flash.each do |key, value| %>
         <p class="alert alert-<%= key %>">
         <%= value %>
         </p>
         <% end %>
     <%=yield %>
   </main>

   <footer>
     <div class="container mt-5">
       <div class="row">
         <div class="mx-auto">
           <p>CopyRight Infratop.inc</p>
         </div>
       </div>
     </div>
   </footer>
  </body>


</html>
